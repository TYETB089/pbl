a] Factorial of Number
Code :
package Exp1;
import java.util.Scanner;
public class Factorial {
public static void main(String[] args) {
int i; int fact=1;
// Now we take input from user
System.out.print("Enter the number : ");
Scanner sc = new Scanner(System.in); 
// Here we create the object of scanner class
int n=sc.nextInt();
for(i=n;i>0;i--){
fact=fact*i;
}
System.out.print("The Factorial is : ");
System.out.println(fact);
} 
}


b] Sum and Average
Code :
package Exp1;
import java.util.Scanner;
public class sum_Avg {
public static void main(String[] args) {
// TODO Auto-generated method stub
 int sum=0;
 // Now we take input from user
 System.out.print("Enter the number : ");
 Scanner sc =new Scanner(System.in);// Here we create the object of scanner 
class
 int n=sc.nextInt();
 // Every iteration sum value is changes and add to the previous value
 for(int i=1;i<=n;i++){
 sum=sum+i;
 }
 System.out.println("The Sum is : " + sum);
 float average = sum/n; // Here we calculate the average of the given sum
 System.out.println("The Average is : " + average);
 
}
}


c] Next 50 prime number
Code : 
package Exp1;
public class First_50_prime {
public static void main(String[] args) {
// TODO Auto-generated method stub
//Here we use two count one for checking the prime number and one for 
number increment to 50
 int cnt=0;int count=0;
 int prime=2;int pr=0;
 while(count<=50){ // this loop for number count
 for(int i=1;i<=prime;i++){ // This for loop for checking the Given 
number is prime or not.
 if(prime%i==0){
 cnt++;
 pr=prime;
 }
 }
 if(cnt==2){ 
 System.out.print(pr+" ");
 count++;
 }
 cnt=0; // here we reassign the the variables for next number 
 prime++;
 
 }
}
}
